# HD2 战报机器人 · 使用说明 v2.1

> 绝地潜兵2（Helldivers 2）QQ 群机器人「真理部自助回复机器人-MTB-114514」（超级地球CN分部）
> 更新日期：2026-08-11 ｜ 覆盖：全部指令、Web 控制台、部署维护、对照表、故障排查

## 一、快速开始

1. 双击 `HD2-Bot\启动HD2推送终端.bat` → 浏览器自动打开 Web 控制台（http://127.0.0.1:8630）
2. 点「🚀 一键启动」→ 自动拉起 AstrBot + NapCat，弹出二维码扫码登录（登录态有效时免扫码）
3. 点「🔍 状态」确认：AstrBot ✅ / WebUI 6185 ✅ / NapCat ✅ / WS 6199 ✅
4. 群里发 `/` 指令即可

## 二、群内指令大全（`/` 前缀，无需 @）

| 指令 | 功能 | 输出 |
| --- | --- | --- |
| `/战报` | 战况速报 | 重要指令（LLM翻译）+ 目标星球战况 + 战区玩家分布 + 最新资讯 |
| `/dss` | DSS 状态 | 停靠星球（中文名）+ 实际生效的战略行动（从停靠星球页面抓取）+ 投票倒计时 |
| `/战役` | 战役列表 | 当前全部战役：星球中文名｜敌方阵营（终结族/机器人/光能族）｜解放战/防御战｜HP |
| `/分析` | LLM 战局分析 | DeepSeek 生成：威胁概述 / 关键目标 / 行动建议 / 风险提示 |
| `/查表` | 变量对照表 | 12 个分类列表 |
| `/查表 <参数名>` | 参数详情 | 如 `/查表 孢裂变种`、`/查表 spore`：译文+原文+分类+描述（中英文均可） |
| `/查表 <分类名>` | 分类子条目 | 如 `/查表 深域惧像`、`/查表 DSS战略行动`：该分类下全部参数 |
| `/查表 星区` | 星区列表 | 全部 55 个星区 |
| `/查表 星区 <星区名>` | 星区星球 | 如 `/查表 星区 巴纳德`：该星区所有星球 |
| `/roll` `/随机战备` | 随机战备 | 全类型战备池随机抽 4 个 |
| `/孢裂变种` 等 | 变种扫描 | 扫描战役星球，输出存在该变种的星球 |
| `/查询 <星球名>` | 星球信息 | 如 `/查询 奥密克戎`、`/奥密克戎信息`：抵抗度/行动变量/入侵等级/POI |

**查询规则**：参数/分类/星区名支持中英文、忽略大小写和空格；查无结果提示「未查询到该参数，请输入「/查表」或「/查表 星系」查看参数。」；星区译名只认对照表已填中文，未填保持英文。

## 三、Web 控制台（http://127.0.0.1:8630）

| 卡片 | 功能 |
| --- | --- |
| 🛠️ 一键启动部署 | 🚀 一键启动（AstrBot+NapCat+二维码+验证）｜📱 打开二维码｜🔄 重启NapCat｜🔁 重启WebUI｜🔍 状态 |
| 🚀 一键发送 | 点按钮直接推送到群：📊 战报 / 🛰️ DSS / ⚔️ 战役 / 🧠 分析 / 📚 查表 / 🎲 Roll / 📖 使用说明 |
| 📊 推送战报 | 生成战报推送到群 |
| 🎲 推送随机战备 | Roll 推送 |
| 🪐 星球信息 | 输入星球名查询并推送 |
| 🧬 变种查询 | 输入变种名扫描并推送 |
| 📢 自定义消息 | 直接推送文本 |
| 🧪 查询推送/响应测试 | 📤 测试并推送｜🧪 仅测试（不发群） |

> 说明：「一键发送/推送」走 NapCat WebUI API 真实发到群；「仅测试」走 WS 注入（不污染群聊）。推送消息主号自己发的不上报机器人（reportSelfMessage=false），测响应用「仅测试」。

## 四、配置（config.json）

所有本机信息集中在 `HD2-Bot\config.json`（含隐私，勿提交 Git）：
- `bot.self_id` 机器人 QQ 号、`bot.groups` 白名单群、`bot.default_group` 默认推送群
- `napcat.token` NapCat WebUI token、`napcat.dir` NapCat 目录
- `astrbot.exe/python` AstrBot 路径
- `llm.api_key` DeepSeek API key（战报 LLM 翻译用）
- `paths.*` 对照表路径

## 五、部署与维护

### 重启 AstrBot（插件改动后生效）
```powershell
Stop-Process -Name "astrbot-desktop-tauri" -Force -ErrorAction SilentlyContinue
Get-CimInstance Win32_Process -Filter "Name='python.exe'" | Where-Object { $_.CommandLine -match 'launch_backend' } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force -ErrorAction SilentlyContinue }
Start-Sleep -Seconds 3
Start-Process "D:\AstrBot\astrbot-desktop-tauri.exe"
```
⚠️ 勿用 `ProcessName -match python` 全杀（会误杀 Web 控制台 8630）。

### 插件部署
- 开发目录：`HD2-Bot\astrbot_plugin_*`（源码）
- 生效目录：`C:\Users\37420\.astrbot\data\plugins\astrbot_plugin_*`（改完复制 + 重启 AstrBot）
- 插件：war_report（战报/dss/战役/分析）、planet_info（星球信息）、lookup（/查表）、roll、variant_query（变种扫描）、guard（指令守卫）

### 对照表维护
- 变量对照表 `tables\HD2行动变量对照表.md`、星区对照表 `tables\星图对照表_修正版.md` → 改完 `/查表` 即时生效（动态读表）
- ⚠️ planet_info 插件词库（VARIANT_CN 等）硬编码，新增词条需同步代码（见 HD2-Bot-Plugin-Dev skill）

## 六、常见问题

| 现象 | 处理 |
| --- | --- |
| WebUI 打不开 / ❌ 网络错误 Failed to fetch | 双击 bat 启动；服务被杀用「🔁 重启WebUI」 |
| 机器人没反应 | 「🔍 状态」WS 是否已连接；白名单：机器人号只收群 YOUR_GROUP_ID，主号收 YOUR_GROUP_ID+YOUR_GROUP_ID |
| QQ 掉线 | 「🔄 重启NapCat」→ 弹二维码重新扫码（先退出 PC 端 QQ） |
| 推送测试发指令机器人不回 | 正常（reportSelfMessage=false）；测响应用「🧪 仅测试」 |
| GitHub/API 无法访问 | hosts 有 #S302 劫持，走 ghproxy.net 镜像 |

## 七、关键路径

- AstrBot：`D:\AstrBot`（WebUI 6185）｜ 配置：`C:\Users\37420\.astrbot\data\`
- 后端日志：`C:\Users\37420\.astrbot\logs\backend.log`
- NapCat：`C:\Users\37420\lobsterai\project\NapCat`（WebUI 6099）｜ 二维码：`napcat\cache\qrcode.png`
- Web 控制台：`HD2-Bot\hd2_webui.py`（8630）｜ 注入测试：`HD2-Bot\napcat_ws_inject.py`
- 推送：`HD2-Bot\napcat_send2.py`（命令行）｜ 开源归档：`HD2-Bot-Release\`
